{{#each model as |item|}}
  <section class="color-group {{item.color}}-bricks">
    <h2>{{item.color}}</h2>
    {{#each item.bricks}}
      <div class="{{if item.onSale 'on-sale'}}">
        <p class="brick-size">Size: {{brick.size}}</p>
        <p class="brick-quantity">Quantity: {{bick.quantity}}</p>
      </div>
    {{/each}}
  </section>
{{/each}}

{{outlet}}
